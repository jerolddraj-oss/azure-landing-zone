# Modules

This directory is reserved for reusable landing-zone modules. Phase 2 will extract networking, policy, identity, monitoring, and workload-spoke patterns into versionable modules after the foundation is validated in East US.
